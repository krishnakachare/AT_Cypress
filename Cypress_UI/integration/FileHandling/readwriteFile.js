///<reference types="cypress"/>

// cy.readFile(filePath, encoding, options)

// cy.writeFile(filePath, contents, encoding, options)   (String, Array, Object)

// cy.readFile(filePath, encoding, options)
// cy.fixture(filepath, encoding, options)

// What is the difference bt cy.fixture & cy.readFile ?

cy.readFile().then(fileData =>{})